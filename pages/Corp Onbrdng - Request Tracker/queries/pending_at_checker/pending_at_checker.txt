select application_id as "Request ID", requestor_email as "Requestor Email", updated_count as "Iteration Count", registered_name as "Organization Name", status as "Request Status" from new_org_requests where status in ('Pending At Checker')