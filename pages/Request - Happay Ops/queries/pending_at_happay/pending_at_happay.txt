select application_id as "Request ID", requestor_email as "Requestor Email", registered_name as "Organization Name", status as "Request Status", updated_count as "Iteration Count", reason as "Reason(If Rejected)" from new_org_requests where status = 'Pending At Happay'