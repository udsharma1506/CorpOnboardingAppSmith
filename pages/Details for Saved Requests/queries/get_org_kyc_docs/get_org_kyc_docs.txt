select id as "Doc ID", created_at as "Creation Date", updated_at as "Updation Date", doc_type as "Document Type", doc_number as "Document Number", right(doc_link,length(doc_link)-69) as "Document Link", status as "Status" from kyc_docs where bo_id = '{{appsmith.store.temp_hpy_req_id}}'